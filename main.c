#include <stdio.h>
#include <stdlib.h>
#include "interface.h"


int main(int argc, char** argv){
    
    int op = 1;
    int tipoGrafo;
    int valorado;

    valorado = menuValorado();
    tipoGrafo = menuTipoGrafo();

    /*
    switch(op){
        case 1:
            break;
        case 2:
            break;
        default:
            printf(" [ ERRO ] Retorno %d", op);
    }
    */

    return 1;
}